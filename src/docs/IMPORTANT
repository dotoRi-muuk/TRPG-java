프로그램의 진행은 다음과 같다.

1. 기술 입력
2. 스탯 입력
3. 데미지 출력

이때, 각 Job 클래스의 함수는 인자로 PrintStream을 받아서 디버그 로그(계산 과정)을 출력하고, 데미지는 int 형으로 반환한다.
아이템을 훔치고, 공격이 원거리로 판정된다던가, 턴을 얻고, 그런 건 알 바 아니다. 이 프로그램은 데미지 계산만 하면 된다. 데미지 계산이 포함되지 않는 스킬은 제작하지 않아도 된다. 구현하지 않을 계획이고.

기본 공격은 1D6으로 계산한다. 모든 데미지에는 (스탯 - 1D20 주사위값) x 10% 의 데미지가 추가된다. 스탯이 정해져 있진 않다. 왜ㅅㅂ

OO 판정(예시: 신속 판정) 같은 경우에는, (스탯- 1D20주사위 값)이 음수이면 실패, 양수이면 성공이다.

굳이 명시되어 있지 않다면, 스태미나를 소모하는 건 기술이고, 마나를 소모하는 건 스킬이다.

javac로 컴파일하지 말 것. class 파일을 지우는 게 번거롭다.